bin/
obj/
*.user
.vs/
.idea/
*.DS_Store
.claude/
.claude/hooks/.walls_grace

# agent 的工作记忆,不是给读者的文档:里面有会话产出、本机绝对路径、provider 细节
PROJECT_STATE.md

# 本机环境变量文件不进 Git；includeSource=true 时 build.txt 也必须忽略它。
# Provider key 应放在 Decision Infra，不应由模组读取。
.env
